# Законы распределения дискретных случайных величин

**Формулировка:**

В службу поддержки приходит в среднем (\lambda=4) письма в час. (X) — число писем за 1 час.

Закон: <img src="https://latex.codecogs.com/svg.image?P(X=k)=e^{-4}\frac{4^k}{k!}" /> (в расчётах взял (k=0\dots14) для таблицы).
Результаты:

- (E(X)=\lambda=4)
- (\mathrm{Var}(X)=\lambda=4)
- (\sigma=2.0)
- мод(ы): при целом (\lambda) две моды: (\lambda-1=3) и (\lambda=4) (в общем для нецелого (\lfloor\lambda\rfloor)).

Таблица и график распределения

![alt text](image.png)

### [Решение](task.py)
